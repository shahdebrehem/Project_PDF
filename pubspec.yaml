name: project_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  # الحزم الأساسية
  dio: ^5.9.2
  shared_preferences: ^2.5.4

  # اختيار الملفات والصور
  file_picker: ^10.3.10
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  file_selector: ^1.1.0

  # دعم اللغات
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true